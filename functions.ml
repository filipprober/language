fn add(a: int, b: int) -> int:
    return a + b

fn multiply(a: int, b: int) -> int:
    return a * b

var sum = add(5, 3)
var product = multiply(2, 4)

if sum > 5:
    var message = "Sum is greater than 5"

if product < 10:
    var message = "Product is less than 10"
